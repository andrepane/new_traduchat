{
  "name": "traduchat-wa",
  "version": "1.0.0",
  "description": "Chat application with translation capabilities",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "if not exist public mkdir public && copy *.html public\\ && copy *.css public\\ && copy *.js public\\ && copy manifest.json public\\ && xcopy /E /I images public\\images && del public\\server.js"
  },
  "dependencies": {
    "express": "^4.17.1",
    "cors": "^2.8.5",
    "body-parser": "^1.19.0",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  },
  "engines": {
    "node": ">=14.0.0"
  }
} 
